---
tags:
  - python
  - strings
  - intro
---

# Python Strings & String Methods — Map of Content

> [!info] Source note
> Strings-as-list-like-sequences (indexing, slicing, `len()`, `in`) was already covered in [[Lists 4 - Tuples & Strings (List-Like Types)]] since the PDF introduced it that way. This series does **not** repeat that — it covers everything else: string literals/escapes, the full method library, formatting, and encoding. None of this is in the provided PDF excerpt, so it's all supplementary.

1. [[Strings 1 - Literals, Escapes & Immutability]] — quotes, escape characters, raw/multiline strings, why strings are immutable (recap + expansion)
2. [[Strings 2 - Case, Search & Whitespace Methods]] — `.upper()`/`.lower()`/`.strip()`/`.find()`/`.count()` etc.
3. [[Strings 3 - Split, Join & Replace]] — `.split()`, `.join()`, `.replace()`, `.partition()`
4. [[Strings 4 - The is Methods & Validation]] — `.isdigit()`, `.isalpha()`, `.isupper()`, etc., and real validation patterns
5. [[Strings 5 - String Formatting]] — f-strings, `.format()`, `%` formatting, format specifiers
6. [[Strings 6 - Beyond the PDF]] — encoding/decoding recap, regex intro, string ↔ bytes for CTF/network work
7. [[Strings 7 - Summary & Self-Test]] — quick-reference table + practice questions

## Related
- [[Lists 4 - Tuples & Strings (List-Like Types)]]
- [[Operators 3 - Type Conversion]]
- [[Python Booleans - MOC]]
